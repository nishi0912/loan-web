import React from 'react';
import { Row, Col, Typography } from 'antd';

const Properties = () => {
  return <Row>
      <Col span={24}>
          <Typography className="admin-building-title">Properties</Typography>
      </Col>
  </Row>;
};

export default Properties;
